import React from 'react';

export const OptionTemplate = () => {
  return (
    <div>
      <span>{title}</span>
      {/* Depending on how many items (generated ul) - <li></li> */}
    </div>
  );
};
